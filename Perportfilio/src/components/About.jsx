import { Container } from 'react-bootstrap';

const About = () => (
  <Container className="py-5 text-white bg-secondary" id="about">
    <h2>About Me</h2>
    <p>
      Former warehouse worker turned Operations Manager — now building full-stack apps with React,
      Node.js, and SQL. Currently pursuing a degree in Computer Science at SNHU while raising a
      family and pushing code every night.
    </p>
  </Container>
);

export default About;
